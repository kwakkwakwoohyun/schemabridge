## 4. 상세 설계 및 개발 환경 구축

> 멘토 피드백 반영 (v2): 매핑정의서·코드 매핑정의서 조회를 RAG(임베딩 검색)에서 Exact Lookup으로 전환하고, 필수조건(타입)→강한 근거(코드값)→보조 근거(명칭 유사도) 순의 판정 로직을 노드 단위로 명시함. No-match/Ambiguous/Version-mismatch 등 판단 불가 상태를 워크플로우에 반영하고, SC-002는 SC-001이 확정된 경우에만 진입하는 확장 흐름으로 재배치함.
>
> 멘토 피드백 반영 (v3): "루프가 없다"는 지적에 따라 `request_clarification` 노드를 추가함. 판정이 애매하거나 근거가 부족하면 즉시 사람에게 넘기지 않고, Agent가 구체적으로 되물어 답변을 받은 뒤 판정 노드로 되돌아가는 루프(최대 2회)를 만듦.
>
> 멘토 피드백 반영 (v4): "메타데이터가 없으면 사람이라면 어떻게 하겠냐"는 질문에 따라, 보조 근거 판정 노드를 확장함. 설명이 없을 때 곧바로 되묻기 루프로 가지 않고, 데이터 샘플 값 > 스키마 제약조건 > 유사 테이블 패턴 > 컬럼명 순으로 자체 추론을 먼저 시도하도록 함(사람의 문제 해결 방식을 모사).
>
> AI 피드백 검토 후 반영 (v5): AI 피드백 중 타당하다고 판단한 것만 선별 반영함 — (1) 개발 환경 구축 섹션을 실제 실행 가능한 수준으로 신설, (2) 노드 입출력에 `source_version`·`confidence_gap` 필드 보강, (3) 판정 임계값을 초기 추정치로 명시(향후 PoC에서 튜닝 예정임을 병기), (4) SC-001 완료 기준을 간단히 명시(상세 골든셋 설계는 6주차 평가 단계로 이관), (5) 내부 추론과 사용자 노출 근거를 분리(Structured Output 도입). 반대로 "임계값을 지금 확정"처럼 실증 데이터 없이 못박기 어려운 항목은 초기값+튜닝 전제로 절충했고, 골든셋 전체 설계처럼 6주차 영역과 중복되는 항목은 이번엔 개요 수준으로만 반영함. **회사 제공 기본 LLM 자원이 Azure OpenAI로 확인되어 모델 선택도 GPT-4o 계열로 변경함.**
>
> 멘토 피드백 반영 (v6): RAG 전략의 임베딩 모델이 로컬 오픈소스(sentence-transformers)로 남아있어 "OpenAI 쓰면서 왜 임베딩만 로컬이냐"는 지적을 받음 — LLM과 임베딩 벤더를 Azure OpenAI로 통일함(Vector DB 미사용 방침은 그대로 유지, 후보 수가 적어 저장·색인이 불필요하기 때문). 아울러 회사 배포 가능 모델이 GPT-5.4까지 확인되어 LLM Model을 GPT-4o 계열에서 GPT-5.4 계열로 상향하고, 문서 작성 시점 기준 최신 라이브러리 버전(LangGraph 1.x, langchain-openai 1.x)으로 핵심 기술 스택을 갱신함.
>
> 실제 값 확정 반영 (v7): 사내 Azure OpenAI 자원을 실제로 발급받아, v6에서 "추후 확정"으로 남겨뒀던 항목들을 확정값으로 교체함 — LLM 배포명은 `gpt-5.6-luna`(v6 예상치였던 GPT-5.4 계열보다 상위), 임베딩 배포명은 `text-embedding-3-large`(v6에서는 `text-embedding-3-small`을 예시로 들었으나 실제로는 large 계열로 발급됨), API 버전은 `2024-12-01-preview`로 확인됨. 엔드포인트 URL은 사내 자원 식별 정보라 이 문서(공개 저장소)에는 적지 않고 `schemabridge/.env`(git 추적 제외)에만 보관함. **`gpt-5.6-luna` 검증:** 이 문서에서 확정한 파싱 전략(Structured Output/JSON Schema strict mode)이 실제로 되는지 골든셋 `wht_tax_amt` 케이스(두 후보 모두 타입 일치·코드매치 없음)로 테스트 호출함 — 스키마 강제 파싱 성공, 판정 결과도 골든셋 정답(`ambiguous`)과 일치, 응답에 `reasoning_tokens`가 잡히는 것으로 보아 추론형 모델로 확인됨. 별도 대안과 비교할 필요 없이 이 배포를 그대로 채택.
>
> **표시 범례: `[v3]` 정보 보완 루프 / `[v4]` 자체 추론(대체 근거) / `[v5]` AI 피드백 선별 반영 + 모델 변경 / `[v6]` 임베딩 벤더 통일 + 모델·라이브러리 버전 최신화 / `[v7]` 실제 Azure OpenAI 값 확정**

### Agent 페르소나 및 시스템 프롬프트 (Identity)

| **항목** | **정의 내용** |
| --- | --- |
| **Agent 이름** | SchemaBridge |
| **주요 역할** | 증권사 차세대(마이그레이션) 프로젝트에서 매핑정의서를 기준으로 AS-IS↔TO-BE 다중 매핑 후보를 판정하고 근거를 제시하는 개발자 보조 Agent (TO-BE SQL 생성은 확정된 매핑을 활용하는 확장 기능) |
| **핵심 목표** | 매핑정의서상 여러 후보가 있는 TO-BE 컬럼에 대해 필수조건→강한 근거→보조 근거 순으로 판정해 근거와 함께 추천한다. **`[v4]`** 설명이 없는 경우 데이터값·제약조건·유사 테이블 패턴·컬럼명으로 먼저 스스로 추론하고, **`[v3]`** 그래도 확신이 낮으면 되물어 정보를 보완한 뒤에만 사람에게 최종 판단을 위임한다 |
| **톤앤매너** | 개발자 대 개발자로 대화하는 간결하고 기술적인 톤. 존댓말을 쓰되 군더더기 없이 핵심 위주로 답변하고, 결론(추천/상태)을 먼저 제시한 뒤 근거를 설명 |
| **제약 사항** | 매핑정의서·스키마에 없는 테이블/컬럼을 임의로 지어내지 않는다. 근거가 불충분한 매핑은 절대 자동 확정하지 않고 반드시 예외 상태로 표시한다. SELECT 이외의 쓰기 쿼리는 절대 생성하지 않는다. **`[v4]`** 근거가 부족하다고 곧바로 사람에게 묻지 않고 이용 가능한 정보로 먼저 스스로 추론을 시도한다. **`[v5]`** 내부 추론 과정 전체를 사용자에게 그대로 노출하지 않고, 정제된 근거(rationale)만 전달한다. 최종 판단은 개발자의 몫임을 분명히 한다 |

### 워크플로우 및 오케스트레이션 (Workflow & Logic)

**2.1 처리 로직**

- **Step 1 (Input Analysis):** TO-BE 컬럼명을 입력받아 매핑 판단 요청(SC-001)인지, 확정된 매핑을 활용한 신고서 집계 요청(SC-002, 확장)인지 분류한다.
- **Step 2 (판정 로직 — 결정적 로직 우선):** (1) 필수조건 — 데이터 타입 불일치 후보는 즉시 탈락(예외 없음). (2) 강한 근거 — 코드 매핑정의서 Exact Lookup으로 코드값 일치 여부 확인. **`[v5]`** 코드값이 일치하는 후보가 하나라도 있으면, 그 후보(들)를 보조 근거 결과와 무관하게 항상 1순위 후보군으로 우선 배치한다. (3) 보조 근거 — 설명이 있으면 유사도, **`[v4]`** 없으면 데이터 샘플 값 > 제약조건 > 유사 테이블 패턴 > 컬럼명 순 자체 추론.
- **Step 3 (판정 및 정보 보완 루프):** 순위를 정한 뒤 **`[v5]`** top1과 top2의 판정 점수 차이(`confidence_gap`)가 임계값(초기값 10%, PoC 진행하며 실데이터로 튜닝 예정) 미만이면 Ambiguous로 판정한다. 자체 추론 확신도가 낮으면 Insufficient-Metadata로 판정한다. 이 두 경우만 **`[v3]`** `request_clarification`으로 되물어 정보를 보완한 뒤 재판정한다(최대 2회). **`[v5]`** Version-mismatch는 매핑정의서상의 AS-IS 컬럼이 최신 TO-BE 스키마 메타데이터 조회 시 아예 존재하지 않을 때만 판정하며, 되묻는다고 해결되는 문제가 아니므로 루프를 타지 않는다.
- **Step 4 (설명 생성 및 응답):** 판정 결과(또는 예외 상태)와 근거를 자연어로 생성해 응답한다. **`[v5]`** 이때 내부 추론 로그(각 도구의 원시 출력)와 사용자에게 노출할 정제된 근거(rationale)를 분리한다 — 내부 추론은 Langfuse 트레이스에 남기고, 사용자 응답에는 구조화된 근거 필드만 노출한다.

**SC-001 완료 기준 (`[v5]` 신규, 개요만 — 상세 골든셋은 6주차에서 설계)**

- 골든셋 최소 12건(1:1 확정 4건, Ambiguous 3건, No-match 2건, Insufficient-Metadata 2건, Version-mismatch 1건) 통과
- 예외 상태 4종(No-match / Ambiguous / Insufficient-Metadata / Version-mismatch)이 실제로 올바르게 분류됨을 확인
- 추천 결과 + 근거 출력 포맷이 고정되어 SC-002가 이 출력을 그대로 입력받을 수 있음
- 이 기준을 충족해야 SC-002(확장) 착수

**2.2 상태 관리**

- 대화 턴 관리: 최근 5턴 윈도우 버퍼. `request_clarification`의 되물음·답변도 같은 버퍼에 포함되어 다음 판정에 즉시 반영됨
- LangGraph Node/Edge 흐름
    - **노드:** `classify_intent` → `lookup_mapping_candidates`(Exact Lookup) → `filter_by_type`(결정적) → `check_code_match`(결정적) → `infer_secondary_evidence`(**`[v4]`** 유사도 + 대체 근거 자체 추론) → `judge_and_rank`(LLM) → **`[v3]`** `request_clarification`(정보 보완 루프) → `generate_rationale`(LLM) → `format_response` / `handle_exception` — SC-002 확장용: `search_schema` → `generate_sql` → `validate_readonly` → `execute_sql` → `self_correct`(재진입)
    - **엣지:**
        - `classify_intent` → (SC-001) `lookup_mapping_candidates` / (SC-002, SC-001 확정 상태일 때만) `search_schema`
        - `lookup_mapping_candidates` → (후보 0개) `handle_exception(No-match)` / (1개 이상) `filter_by_type`
        - `filter_by_type` → (전체 제외) `handle_exception(No-match)` / (1개 이상 남음) `check_code_match`
        - `check_code_match` → `infer_secondary_evidence` → `judge_and_rank` → (명확) `generate_rationale` → `format_response` / (AS-IS 컬럼이 최신 스키마에 없음) `handle_exception(Version-mismatch)` / **`[v5]`** (`confidence_gap` < 임계값 또는 확신도 낮음, 재시도<2) `request_clarification`
        - **`[v3]`** `request_clarification` → (응답 받음) `judge_and_rank` 재진입(루프) / (재시도≥2, 미해결) `handle_exception(Ambiguous 또는 Insufficient-Metadata)`
        - (SC-002) `search_schema` → `generate_sql` → `validate_readonly` → (통과) `execute_sql` / (위반) `fail_response`
        - `execute_sql` → (성공) `format_response` / (실패, 재시도<3) `generate_sql` / (실패, 재시도≥3) `fail_response`
    - **state 예시:** `{to_be_column, candidates, filtered_candidates, code_match_results, evidence_scores, evidence_source, ranked_result, confidence_gap, exception_status, clarification_attempts, clarification_answers, rationale, sql, attempt_count, final_answer}`

### 도구(Tools) 및 함수 명세 (Capability)

| **도구명** | **기능 설명** | **입력** | **출력** |
| --- | --- | --- | --- |
| `lookup_mapping_candidates` | 매핑정의서를 정확 조회(Exact Lookup)해 AS-IS 후보 목록을 조회 | `to_be_column: str` | `candidates: list[{table, column, description, type}]`, **`[v5]`** `source_version: str`(매핑정의서 버전, Version-mismatch 판정용) |
| `filter_by_type` | 데이터 타입 불일치 후보를 제외하는 필수조건 필터 | `candidates: list`, `to_be_type: str` | `filtered: list`, `excluded: list[{candidate, reason}]` |
| `check_code_match` | 코드 매핑정의서를 정확 조회해 코드값 일치 여부 확인(강한 근거) | `candidates: list` | `code_match_results: list[{candidate, matched: bool}]` |
| `infer_secondary_evidence` (구 `compute_similarity`) | 설명이 있으면 유사도, 없으면 데이터 샘플 값 > 제약조건 > 유사 테이블 패턴 > 컬럼명 순으로 자체 추론 | `candidates`, `to_be_description`(optional), `sample_data`, `constraints`, `similar_confirmed_mappings` | `scores: list[{candidate, score, evidence_source}]`, `confidence: float` |
| `judge_and_rank` | 근거를 종합해 순위 결정 | `code_match_results`, `scores`, `confidence` | `ranked_result`, **`[v5]`** `confidence_gap: float`(top1-top2 점수차), `status: "confirmed"/"ambiguous"/"insufficient_metadata"/"no_match"/"version_mismatch"` |
| `request_clarification` **`[v3]`** | 부족한 근거를 구체적으로 되묻고 답변을 받음(최대 2회) | `ranked_result`, `reason` | `user_response: str`, `attempt_count: int` |
| `generate_rationale` | 최종 근거 설명 생성. **`[v5]`** 내부 추론(raw 값)과 사용자 노출용 문장을 분리해 후자만 반환 | `ranked_result`, `status`, `clarification_answers`(optional) | `rationale: str`(사용자 노출용) |
| `search_schema` (SC-002) | RAG로 TO-BE 스키마 조각 검색 | `query`, `k` | `schema_chunks: list[str]` |
| `generate_sql` (SC-002) | TO-BE SQL 생성 | `schema_context`, `query`, `error_feedback`(optional) | `sql`, `thought_process`(내부 로그용) |
| `validate_readonly` (SC-002) | 읽기 전용 검증 | `sql` | `is_valid`, `reason` |
| `execute_sql` (SC-002) | 쿼리 실행 | `sql` | `columns`, `rows`, `error`(optional) |

### 지식 베이스 및 메모리 전략 (Context & Memory)

**4.1 RAG 전략**

- **참조 데이터 소스:** 테이블/컬럼 매핑정의서, 코드 매핑정의서, TO-BE·AS-IS 스키마 메타데이터, 데이터 샘플 값, 스키마 제약조건, 확정된 매핑 결과(유사 테이블 패턴용), 신고서 양식 정의(확장용)
- **조회 방식 구분:** 매핑정의서·코드 매핑정의서는 정확 조회(Exact Lookup). 컬럼명·설명과, 설명이 없을 때의 대체 근거(데이터값·제약조건·유사 테이블 패턴)는 임베딩/LLM 기반 추론
- **청킹 방식:** 매핑정의서는 청킹 대상 아님. `infer_secondary_evidence`에서만 후보 3~5개 수준을 즉석 비교
- **임베딩 모델:** **`[v7 확정]`** Azure OpenAI Embeddings `text-embedding-3-large`(v6에서는 `text-embedding-3-small`을 예시로 들었으나 실제 발급된 배포는 large 계열) — LLM과 동일한 벤더로 통일해 별도 API·로컬 모델 관리 부담을 없앰
- **Vector DB:** 사용하지 않음(후보 3~5개 수준을 즉석 비교만 하므로 대량 저장·색인용 인프라가 불필요)

**4.2 대화 메모리**

- 최근 5턴 윈도우 버퍼. `request_clarification`의 되물음·답변 포함
- 로컬 개발 도구 특성상 세션 중에만 메모리 유지, 종료 시 초기화

### 개발 환경 구축 (`[v5]` 신규 섹션)

| 항목 | 내용 |
| --- | --- |
| **Python 버전** | 3.11 이상 |
| **패키지 관리** | `uv` (빠른 설치·잠금 파일 관리. 기존처럼 `pip install -r requirements.txt`도 병행 지원) |
| **핵심 라이브러리(예상 버전)** | **`[v6 변경]`** `langgraph>=1.2`(구 `>=0.2`에서 상향, 문서 작성 시점 최신은 1.2.10), `langchain-openai>=1.5`(Azure OpenAI 연동 + 임베딩 호출 겸용), `openai>=1.40`, `python-dotenv>=1.0`, `langfuse>=2.0`, `rich`(CLI 출력) — `sentence-transformers`는 임베딩을 Azure OpenAI로 통일하며 제거 |
| **실행 엔트리포인트** | 데모 데이터 생성: `python data/setup_demo_data.py` / 대화형 실행: `python -m src.cli` / 평가: `python tests/run_eval.py` |
| **환경변수** | `AZURE_OPENAI_API_KEY`, `AZURE_OPENAI_ENDPOINT`, `AZURE_OPENAI_DEPLOYMENT_NAME`(**`[v7 확정]`** `gpt-5.6-luna`), `AZURE_OPENAI_EMBEDDING_DEPLOYMENT_NAME`(**`[v7 확정]`** `text-embedding-3-large`), `AZURE_OPENAI_API_VERSION`(**`[v7 확정]`** `2024-12-01-preview`), `LANGFUSE_PUBLIC_KEY`, `LANGFUSE_SECRET_KEY`(선택, 모니터링용) — 실제 값은 `schemabridge/.env`(git 추적 제외)에 보관, 변수 목록만 `schemabridge/.env.example`에 공개 |
| **로컬 데이터 저장** | SQLite(TO-BE 실행용 DB, SC-002에서 실제 쿼리 실행 대상), JSON(매핑정의서·코드 매핑정의서·데이터 샘플·제약조건 등 합성 데이터) |
| **샘플 데이터 적재 방식** | `data/setup_demo_data.py` 한 번 실행으로 합성 스키마·매핑정의서·골든셋을 모두 생성/적재 (2주차 스캐폴드의 `db_setup.py` 방식을 확장) |
| **로그/트레이싱 설정** | Langfuse SDK를 LangGraph 콜백으로 연결. 환경변수 설정 시 자동으로 노드별 실행 순서·소요시간·LLM 호출 내용이 Langfuse 대시보드에 트레이스로 기록됨. 미설정 시 콘솔 로그로 폴백 |

### 핵심 에이전트 기술 스택

| **구분** | **선정 전략/기술** | **선정 사유** |
| --- | --- | --- |
| **LLM Model** | **`[v7 확정]`** Azure OpenAI 배포 `gpt-5.6-luna` (v6에서 "추후 확정"으로 남겼던 배포 버전을 실제 발급받아 확정. GPT-5.4 계열로 예상했던 것보다 상위 버전이며, 추론형(reasoning) 모델로 확인됨) | 회사가 제공하는 표준 자원이라 별도 API 키·과금 없이 바로 쓸 수 있고, 평가자 입장에서도 재현 가능성이 높음. Structured Output 테스트 호출로 파싱 전략과의 호환성도 확인함 |
| **Embedding Model** | **`[v7 확정]`** Azure OpenAI Embeddings `text-embedding-3-large` (v6 예시였던 `text-embedding-3-small`에서 실제 발급 배포로 교체) | LLM과 동일한 벤더로 통일해 별도 로컬 모델·API를 관리할 필요가 없어짐. 후보 3~5개를 즉석 비교하는 용도라 대규모 임베딩 인프라는 불필요 |
| **Retrieval 전략** | Exact Lookup + 경량 임베딩 유사도 + **`[v4]`** 다단계 자체 추론 병행 | 정형 데이터는 정확 조회, 설명이 없으면 사람처럼 여러 근거를 종합해 스스로 추론하고, 그래도 확신이 낮을 때만 되묻는 구조 |
| **Agent Framework** | LangGraph (**`[v6]`** `>=1.2`) | 조건부 분기, 예외 상태 처리, **`[v3]`** 정보 보완 루프까지 노드/엣지로 명시 가능. **`[v5]`** LLM 벤더를 Claude에서 OpenAI로 바꿔도 그래프 구조는 그대로 유지되는 것도 장점 |
| **Prompt Strategy** | Few-shot + Chain-of-Thought (내부 추론용) | 판정 정확도를 높이기 위해 내부적으로는 사고 과정을 거치되, **`[v5]`** 이 추론 전체를 사용자에게 노출하지 않고 로그로만 남김 |
| **Output Parsing** | **`[v5 변경]`** Structured Output(JSON Schema 강제) — 기존 XML 태그 방식에서 전환 | **`[v7 확정]`** `gpt-5.6-luna` 배포로 실제 테스트 호출해 JSON Schema strict mode가 정상 동작함을 확인함(추정이 아닌 검증 완료). `judge_and_rank`의 `ranked_result`/`confidence_gap`/`status` 같은 필드를 스키마로 강제해 파싱 실패 위험을 줄이고, 내부 추론(내부 필드)과 사용자 노출 근거(rationale 필드)를 스키마 수준에서 분리 |
| **Monitoring** | Langfuse | 무료 오픈소스, LangGraph 연동, 루프 반복 횟수도 트레이스에서 바로 확인 가능 |
