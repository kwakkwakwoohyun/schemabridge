## 3. 시나리오 수립

> 멘토 피드백 반영 (v2): SC-001의 후보 판정 규칙을 필수조건/보조조건으로 구체화하고, 실패·예외 흐름을 추가함. SC-002는 SC-001의 확장 시나리오로 재배치하고 범위를 제한된 Vertical Slice로 좁힘. 성공 기준을 테스트 케이스 유형별로 분리함.
>
> 멘토 피드백 반영 (v3): SC-001에 "정보 보완 루프"를 추가함 — 판단이 애매하거나 근거 정보가 부족할 때 즉시 사람에게 넘기지 않고, Agent가 구체적으로 되물어 정보를 보완한 뒤 재판정을 시도하도록 함. 메타데이터(설명·코드매핑 등)가 없는 경우의 흐름을 신규 예외 유형으로 추가함.
>
> 멘토 피드백 반영 (v4): "메타데이터(설명)가 없으면 사람이라면 어떻게 행동했을까"를 다시 고민함. 사람은 설명이 없어도 바로 남에게 묻지 않고 데이터 값·제약조건·비슷한 테이블 패턴·컬럼명 등을 먼저 스스로 훑어보고 추론하므로, Agent도 동일하게 동작하도록 보조 근거 판정 단계를 확장함. 이건 새로운 루프가 아니라 기존 보조 근거 판정을 더 풍부하게 만드는 것이며, "정보 보완 루프(사람에게 되묻기)"는 이 자체 추론까지 실패했을 때만 타는 최후 수단으로 재배치됨.
>
> 구현 중 피드백 반영 (v5): PoC 구현 착수 후 실제 코드를 만들면서 드러난 SC-002 범위 확장분을 반영함(4주차_완료.md의 "구현 착수 반영" 항목들과 같은 성격 — 설계 문서 작성 시점엔 몰랐던 부분이 구현 중 지적되어 바로잡힘). (1) 신고서 대상 소득유형이 실무에서 기타·사업소득뿐 아니라 배당소득까지 포함되는 게 자연스럽다는 지적을 받아들여 범위를 배당·기타·사업소득 3종으로 넓힘. (2) "TO-BE 쿼리 생성"이 실제로는 AS-IS 원장 데이터를 매 쿼리마다 실시간으로 취합하는 게 아니라, 차세대 마이그레이션이 끝나 TO-BE 테이블에 데이터가 이미 반영되어 있다고 보고 그 위에서 조회하는 그림이 맞다는 지적을 받아 사전 조건 문구를 바로잡음. (3) "관련 테이블이 뭔지 알려줘" 같은 탐색 요청까지 매번 쿼리를 만들어 실행해버리는 게 부적절하다는 지적을 받아, 탐색(정보 조회)과 실행(집계 데이터 생성)을 구분하는 흐름을 추가함. (4) 신고서는 항상 특정 기간 단위인데 정작 기간 조건을 걸 수 없었다는 지적을 받아, 고정 신고서 항목에 지급일자를 추가하고 기간 조건 처리를 명시함.
>
> **표시 범례: `[v3 신규/수정]` = 3번째 피드백 반영분 / `[v4 신규/수정]` = 4번째 피드백 반영분 / `[v5 신규/수정]` = 5번째 피드백 반영분(PoC 구현 중 드러난 SC-002 범위 확장)** (회의에서 "이번에 뭘 바꿨나" 물어보면 이 표시만 따라가면 됨)

### 핵심 사용자 시나리오

**시나리오 1 : 다중 후보 매핑 판단 지원 (필수)**

- ID : SC-001
- 상황 : TO-BE 회계 집계 테이블 컬럼을 채우기 위해 타 팀(계정계 원장) 소관의 원천징수 테이블(근로/이자/배당 등)을 참조해야 하는데, 매핑정의서상 해당 컬럼이 여러 AS-IS 후보 테이블/컬럼에 매핑되어 있어 어떤 값을 가져와야 할지 판단하기 어려운 상황
- 목표 : 여러 매핑 후보 중 가장 적합한 테이블/컬럼을 근거와 함께 추천받아 판단 시간을 줄인다
- 사전 조건 : 개발자가 작성해 둔 테이블/컬럼 매핑정의서와 코드 매핑정의서가 Agent에 입력(색인)되어 있어야 함. **`[v3 신규]`** 정보 보완 루프를 위해 사용자와 대화형으로 되묻고 답변받을 수 있는 인터페이스(CLI 등)가 필요함. **`[v4 신규]`** AS-IS/TO-BE 스키마에는 컬럼 설명뿐 아니라 데이터 샘플 값과 제약조건(FK·NOT NULL·컬럼 길이 등) 정보도 함께 준비되어 있어야 함(설명이 없을 때의 대체 근거로 사용)
- 상세 흐름

| 단계 | 사용자 행동 | 시스템 동작 | 비고 |
| --- | --- | --- | --- |
| 1 | TO-BE 컬럼(예: 원천징수세액)을 입력해 매핑 후보를 조회 요청 | 매핑정의서를 정확 조회(Exact Lookup)해 해당 컬럼과 매핑된 모든 AS-IS 후보(근로/이자/배당 원천징수 테이블 등)를 조회 | 후보가 여러 개 존재하는 경우에 해당 |
| 2 | (자동 진행, 별도 행동 없음) | 후보 판정 규칙을 순서대로 적용: (1) 필수조건 — 데이터 타입이 불일치하는 후보는 제외 (2) 강한 근거 — 코드 매핑정의서상 코드 체계(코드값)가 일치하는 후보를 우선순위 상단에 배치 (3) 보조 근거 — 컬럼 설명이 있으면 설명 유사도로 판단. **`[v4 신규]`** 설명이 없으면 데이터 샘플 값 > 스키마 제약조건(FK·NOT NULL·컬럼 길이 등) > 구조가 유사한 다른 테이블의 매핑 패턴 > 컬럼명, 이 우선순위대로 근거들을 한 번에 종합해 LLM이 추론(사람이 설명 없이 컬럼 의미를 유추하는 방식을 모사) | 필수조건 미충족 후보는 순위에서 제외하고 사유를 기록. **`[v4 신규]`** 여러 대체 근거가 서로 다른 결론을 가리키면 우선순위가 높은 근거를 더 신뢰 |
| 3 **`[v3 신규]`** | (조건부) 시스템의 되물음에 추가 정보로 답변 | 판정이 애매(Ambiguous)하거나 근거 정보 일부가 없어(메타데이터 부족) 신뢰도가 낮으면, 무엇이 부족한지 구체적으로 되묻고 답변을 반영해 2단계 판정을 재수행 (최대 2회 반복) | 정보 보완 루프. 후보 없음·버전 불일치·타입 충돌처럼 되물어도 해결되지 않는 예외는 이 루프를 타지 않고 바로 4단계로 감 |
| 4 (기존 3단계) | 추천 결과와 근거를 확인하고 최종 매핑을 선택 | 1순위 추천과 근거(참조 Source, 적용된 매핑 규칙, 판단 이유, 루프를 거쳤다면 보완된 정보)를 함께 제시. **`[v3 신규]`** 재시도 한도 내에도 해결 안 되면 후보 전체를 제시 | 최종 판단은 개발자가 수행, Agent는 매핑정의서를 새로 만드는 것이 아니라 기존 매핑정의서를 기반으로 판단을 보조 |
- 입력 예시

"TO-BE 원천징수세액 컬럼에 대응하는 AS-IS 컬럼 후보를 찾아줘"

**`[v3 신규]`** (정보 보완 루프 예시) Agent: "배당소득 원천징수 테이블의 세액 컬럼에 설명이 없어 판단이 어렵습니다. 이 컬럼이 원천징수세액과 같은 항목인가요?" → 사용자: "네, 같은 항목 맞아요" → Agent가 이 답변을 반영해 재판정

**`[v4 신규]`** (자체 추론 성공 예시, 루프 없이 해결) 배당소득 원천징수 테이블의 `DIV_WHT_AMT` 컬럼에 설명이 없지만 → 데이터 샘플이 금액 형태 값이고, 근로소득 테이블의 동일 구조 컬럼(`LBR_WHT_AMT`, 이미 매핑 확정됨)과 컬럼명 패턴·제약조건이 동일함을 근거로 자체 추론 → 사람에게 되묻지 않고 바로 "원천징수세액"으로 추천 (근거에 "설명 없음, 데이터 샘플·유사 테이블 패턴으로 추론"이라고 명시)

- 기대 출력

후보 3개(근로소득 원천징수 테이블.세액, 이자소득 원천징수 테이블.세액, 배당소득 원천징수 테이블.세액)와 각 후보의 판정 근거(필수조건 통과 여부, 코드값 일치 여부, 명칭 유사도, **`[v3 신규]`** 정보 보완 루프로 확인된 내용이 있다면 그것도 포함, **`[v4 신규]`** 설명이 없는 후보는 어떤 대체 근거(데이터값/제약조건/유사 테이블 패턴/컬럼명)로 추론했는지도 함께 표시)·추천 신뢰도(%), 1순위 추천 결과

- 예외/실패 처리
    - 후보 없음(No-match): 매핑정의서에 해당 TO-BE 컬럼 자체가 없으면 "매핑 정보 없음"으로 표시하고, 신규 매핑 등록이 필요함을 안내. **`[v3 신규]`** (정보 보완 루프 대상 아님 — 애초에 물어볼 후보가 없음)
    - 후보 모호(Ambiguous): 강한 근거(코드값 일치)까지 적용해도 복수 후보의 우선순위가 동률이면, **`[v3 수정]`** 먼저 정보 보완 루프로 구체적인 되물음을 최대 2회 시도. 그래도 안 풀리면 동률 후보 전체를 제시하며 사람의 선택을 요청
    - 메타데이터 부족(Insufficient Metadata): 컬럼 설명이 없는 경우, **`[v4 수정]`** 먼저 데이터 샘플 값 > 제약조건 > 유사 테이블 패턴 > 컬럼명 순으로 자체 추론을 시도한다(사람이 문서 없이 하는 방식과 동일). 자체 추론으로 확신이 낮을 때만 정보 보완 루프로 넘어가 부족한 항목을 사용자에게 구체적으로 요청해 재판정을 시도하고, 그래도 확보되지 않으면 확인하지 못한 항목을 명시한 채 이용 가능한 정보만으로 잠정 순위와 낮은 신뢰도를 제시
    - 매핑정의서-ERD 버전 불일치: 매핑정의서상의 컬럼이 최신 TO-BE ERD에 없으면 "버전 불일치 경고"를 표시하고 최신화 필요 여부 확인을 요청. **`[v3 신규]`** (구조적 문제라 정보 보완 루프 대상 아님)
    - 타입/코드값 충돌: 필수조건(데이터 타입)을 통과하는 후보가 하나도 없으면 후보 없음과 동일하게 처리하고 충돌 사유를 함께 기록. **`[v3 신규]`** (명확한 배제 로직이라 루프 대상 아님)
- 성공 기준
    - 기준 1: 1:1 매핑 사례에서 Top-1 정확도(정답이 1순위와 정확히 일치)를 별도로 측정
    - 기준 2: 1:N·모호 후보 사례를 포함해 Top-2 포함률(정답이 상위 2개 안에 포함)을 별도로 측정
    - 기준 3: No-match, Ambiguous, Insufficient Metadata, Version-mismatch, Type Conflict 예외 사례가 실제로 올바른 예외 상태로 분류되는 비율을 측정
    - 기준 4: 추천 근거에 참조 Source·적용된 매핑 규칙·판단 이유가 모두 포함되어 있는지 체크리스트로 확인
    - 기준 5 **`[v3 신규]`**: 정보 보완 루프를 거친 사례에서, 루프 이전과 이후의 판정 정확도(또는 신뢰도) 차이를 측정해 루프가 실제로 판단을 개선하는지 확인
    - 기준 6 **`[v3 신규]`**: 정보 보완 루프가 최대 시도 횟수(2회) 내에서 항상 종료되고 무한 반복되지 않는지 확인
    - 기준 7 **`[v4 신규]`**: 설명이 없는 케이스 중, 정보 보완 루프(사람에게 되묻기) 없이 자체 추론(데이터값/제약조건/유사 테이블 패턴/컬럼명)만으로 올바르게 판정되는 비율(자기추론 해결률)을 별도로 측정 — 이 비율이 높을수록 사람에게 되묻는 빈도가 줄어드는 것이므로 실용성 지표로 삼음

**시나리오 2 : 신고서용 집계 쿼리 생성 (SC-001 결과를 활용하는 확장 시나리오)**

- ID : SC-002
- 상황 : 원천세 신고서 중에서도 **`[v5 수정]`** 배당·기타·사업소득 유형에 한해, 차세대 마이그레이션이 끝나 각 소득유형별 TO-BE 집계 테이블에 이미 반영되어 있는 데이터를 정해진 신고서 양식대로 정리해야 하는 상황(초안에는 "계정계 원장 데이터를 그때그때 취합"이라고 적었으나, 실무에서는 마이그레이션이 끝나면 TO-BE 테이블에 데이터가 이미 채워져 있는 게 맞는 그림이라 수정함)
- 목표 : SC-001에서 확정된 매핑 결과와, 미리 정의된 조인 규칙·고정된 신고 항목을 바탕으로 TO-BE 쿼리를 생성받는다 (자유 형식 전체 자동 생성이 아닌 제한된 범위의 Vertical Slice). **`[v5 신규]`** 다만 매번 쿼리 생성·실행까지 갈 필요는 없다 — "관련 테이블이 뭔지 알려줘" 같은 탐색 요청이면 쿼리를 만들지 않고 관련 TO-BE 테이블/컬럼 정보만 안내한다
- 사전 조건 : **`[v5 수정]`** 배당·기타·사업소득 관련 TO-BE 테이블 스키마(마이그레이션 완료 후 데이터가 채워져 있다고 가정), SC-001에서 확정된 매핑 결과, 사전 정의된 조인 규칙, 고정된 신고서 양식 항목이 준비되어 있어야 함
- 상세 흐름

| 단계 | 사용자 행동 | 시스템 동작 | 비고 |
| --- | --- | --- | --- |
| 1 | "2026년 1분기 원천세(배당·기타·사업소득) 신고서용 집계 데이터 뽑아줘"와 같이 요청, 또는 **`[v5 신규]`** "원천세 집계 관련된 테이블 다 찾아줘"처럼 정보만 묻는 요청 | SC-001에서 확정된 매핑 결과와 사전 정의된 조인 규칙을 조회 (매핑이 미확정 상태면 SC-001부터 먼저 진행하도록 안내). **`[v5 신규]`** 요청이 탐색(정보 조회)인지 실행(집계 데이터 생성)인지 먼저 판단해, 탐색이면 다음 단계로 가지 않고 여기서 관련 테이블/컬럼 정보만 응답 | 자유 검색이 아닌, 확정된 매핑·고정 규칙 기반 |
| 2 | (자동 진행, 별도 행동 없음) | 고정된 신고서 항목(**`[v5 수정]`** 지급일자, 지급처, 소득구분, 소득금액, 원천징수세액)에 맞춰 조인·집계 TO-BE SQL을 생성. **`[v5 신규]`** 요청에 특정 기간(분기/연도)이 있으면 지급일자 기준 조건을 추가하고, 없으면 전체 기간을 조회 | **`[v5 수정]`** 배당·기타·사업소득 유형으로 범위 한정 |
| 3 | 생성된 SQL을 실행하고 결과를 검토 | Read-only 검증 → 실행 → **`[v3 신규]`** (실패 시 에러를 반영해 SQL을 재생성하는 자기수정 루프, 최대 3회) → 결과를 신고서 양식 레이아웃으로 정리해 반환 | **`[v3 신규]`** 이 시나리오는 실행 실패 시 재시도 루프를 이미 포함하고 있음 |
- 입력 예시

"2026년 1분기 원천세(배당·기타·사업소득) 신고서용 집계 데이터 뽑아줘"

**`[v5 신규]`** (탐색 요청 예시, 쿼리 생성/실행 없음) "원천세 집계 관련된 테이블 다 찾아줘" → 관련 TO-BE 테이블 3개(배당/기타/사업소득)와 각 컬럼 매핑 정보만 안내

- 기대 출력

**`[v5 수정]`** 지급일자, 지급처명, 소득구분, 소득금액, 원천징수세액 등 신고서 항목별로 정리된 표와 생성된 TO-BE SQL

- 예외/실패 처리
    - SC-001 매핑이 미확정(모호/후보없음/**`[v3 수정]`** 메타데이터 부족) 상태: SQL 생성을 진행하지 않고 SC-001에서 먼저 매핑을 확정하도록 안내
    - SQL 실행 실패: **`[v3 수정]`** 에러 메시지를 반영해 최대 3회까지 자기수정 재시도. 그래도 실패하면 오류 내용을 표시하고 Read-only 검증·조인 규칙을 재확인하도록 안내 (임의 재작성으로 자동 확정하지 않음)
    - 실행 결과가 기대값(정답 데이터)과 다른 경우: 결과 일치 여부는 자동 승인 대상이 아니므로 "검토 필요"로 표시하고 개발자 확인을 거치도록 함
- 성공 기준
    - 기준 1: 문법 통과율
    - 기준 2: Read-only 검증 통과율
    - 기준 3: 실행 성공률 (**`[v3 수정]`** 자기수정 재시도 포함)
    - 기준 4: 신고서 필수 항목 포함률
    - 기준 5: 결과값 일치율 (참고 지표로 별도 기록, 자동 합격 기준에는 포함하지 않음)

### 시나리오 우선순위 매트릭스

| 시나리오 | 비즈니스 가치 | 구현 난이도 | PoC 포함 |
| --- | --- | --- | --- |
| SC-001 | 높음 | 높음 | 네 (필수 데모) |
| SC-002 | 중간 | 중간 | 부분적 (SC-001 완료 후, 배당·기타·사업소득 한정 Vertical Slice로 확장 시도) |

---

### 회의 대비 요약 — "이번에 뭘 추가했나?" 한 줄 답변

- **왜 바꿨나:** 멘토가 "루프가 없다"와 "메타데이터 없을 때 흐름이 없다" 두 가지를 지적함
- **뭘 추가했나:** SC-001에 "정보 보완 루프" 하나를 새로 넣어서 두 지적을 동시에 해결함 — 판단이 애매하거나(Ambiguous) 근거 정보가 부족하면(메타데이터 부족, 신규 예외 유형) 곧바로 사람에게 넘기지 않고, Agent가 부족한 부분을 콕 집어 되물은 뒤(최대 2회) 답변을 반영해 재판정. 그래도 안 풀리면 그때 사람에게 넘김
- **왜 이 방식인가:** 후보없음·버전불일치·타입충돌처럼 "물어봐도 해결 안 되는" 예외는 루프를 안 태우고, "물어보면 해결될 가능성이 있는" 애매함·정보부족만 루프를 태우도록 구분함
- **검증 방법도 추가함:** 루프를 거친 사례가 실제로 정확도를 개선하는지(기준 5), 무한반복 없이 종료되는지(기준 6)를 성공 기준에 넣음

### 회의 대비 요약 — v4 (자체 추론 단계 추가)

- **멘토가 물어본 것:** "메타데이터가 없으면 사람이라면 어떻게 행동하겠냐"는 질문. 사람은 설명이 없어도 바로 남에게 안 묻고 스스로 먼저 추론해본다는 게 포인트
- **뭘 추가했나:** 새 루프가 아니라, 기존 "보조 근거" 판정 단계를 확장함. 설명이 없으면 데이터 샘플 값 > 스키마 제약조건 > 구조가 비슷한 다른 테이블의 패턴 > 컬럼명, 이 우선순위로 근거를 모아 LLM이 한 번에 종합 추론하도록 함
- **왜 루프가 아니라 단일 단계인가:** 루프는 "같은 걸 반복 시도"할 때 필요한 건데, 이건 여러 근거를 동시에 놓고 한 번에 판단하는 것이라 반복이 아님. "정보 보완 루프(사람에게 되묻기)"는 이 자체 추론까지 실패했을 때만 타는 최후 수단으로 유지
- **효과:** 사람에게 되묻는 빈도 자체가 줄어들고, Agent가 실제 사람의 문제해결 방식을 모사한다는 설계 근거가 생김 (기준 7로 자기추론 해결률을 측정)

### 회의 대비 요약 — v5 (SC-002 구현 중 드러난 범위 확장)

- **왜 바꿨나:** 설계 문서 작성 시점엔 몰랐던 부분들이 SC-002를 실제로 구현하면서 드러남 — 소득유형 범위(배당소득 누락), 데이터가 어디서 오는지에 대한 그림(AS-IS 실시간 취합 vs 이미 마이그레이션된 TO-BE 조회), 탐색과 실행을 구분 안 함, 기간 조건을 걸 수 없음
- **뭘 바꿨나:** (1) 대상 소득유형을 배당·기타·사업소득 3종으로 넓힘 (2) "TO-BE 쿼리 생성"의 전제를 "AS-IS 데이터를 그때그때 취합"에서 "마이그레이션이 끝나 TO-BE 테이블에 데이터가 이미 있다"로 바로잡음 (3) "테이블만 알려줘" 같은 탐색 요청이면 쿼리를 만들지 않고 정보만 응답하는 흐름 추가 (4) 고정 신고서 항목에 지급일자를 추가해 기간(분기/연도) 조건이 실제로 걸리게 함
- **왜 이 방식인가:** 셋 다 "설계 의도는 맞는데 세부 구현이 실무 감각을 놓친" 케이스라, 시나리오 자체를 새로 짜기보다 기존 흐름에 빠진 조각을 채우는 방식으로 수정함
- **범위 밖으로 남긴 것:** 기간 표현("이번 분기" 등) 해석은 실행 시점의 실제 날짜를 기준으로 하는 상대 표현까지만 지원 — 회계연도 기준이 실제 달력연도와 다른 경우 같은 세부 규칙은 이번 Vertical Slice 범위 밖
