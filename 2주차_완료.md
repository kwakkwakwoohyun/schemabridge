## 2. 문제 정의 및 서비스 기획

> 멘토 피드백 반영 (v2): 범위를 다중 매핑 후보 판단 지원 중심으로 좁히고, SQL 생성은 확장 기능으로 축소함. KPI 판정 기준을 구체화하고, 판단 불가 상태 처리를 핵심 기능에 추가함.

### 프로젝트 명

AS-IS/TO-BE 매핑 판단 지원 Agent

### 배경 및 문제 정의

- 현재 상황 (As-Is): 증권사 차세대(마이그레이션) 프로젝트에서는 AS-IS 코드를 분석해 TO-BE ERD를 설계하고, 이후 테이블 매핑정의서·컬럼 매핑정의서를 엑셀로 수작업 작성한다. TO-BE 쿼리를 개발할 때마다 개발자는 AS-IS 코드와 이 매핑정의서를 일일이 열어 대조하며 작업한다.
- Pain Point: 매핑 문서가 많아 하나씩 찾아 열어보는 데만도 시간이 오래 걸려 개발 속도가 느려진다. 또한 AS-IS:TO-BE 관계가 항상 1:1이 아니라 설계 변경으로 1:N으로 쪼개지거나 프로세스 자체가 바뀌는 경우가 있는데, 이를 사람이 그때그때 판단해서 반영해야 해서 개발 시간이 더 늘어나고 실수 위험도 커진다.
- 기회 요인: 매핑정의서를 Agent가 참조하고 있으면, 한 TO-BE 컬럼에 AS-IS 후보가 여러 개일 때 근거와 함께 최적 후보를 추천해 판단 시간을 줄일 수 있다. 근거가 부족하거나 후보가 충돌하는 경우까지 명시적으로 구분해주면, 개발자가 어디까지 믿고 어디부터 직접 확인해야 하는지 분명해진다.

### 목표 사용자 (Target User)

증권사 차세대(마이그레이션) 프로젝트에서 TO-BE 쿼리를 개발하는 SI 개발자 (1차 기준: 본인). AS-IS 코드와 매핑정의서를 대조해가며 TO-BE 쿼리를 작성해야 하는 업무 특성을 가짐.

### 프로젝트 목표

- 정성적 목표
    - 목표 1: 매핑 문서 수작업 대조 작업 자동화
    - 목표 2: 근거 기반 매핑 판단으로 구조 변경(1:1→1:N 등) 관련 개발 실수 예방
    - 목표 3: AI 에이전트 개발 역량 내재화
- 정량적 목표 (KPI)

| **성과 지표** | **목표 수치** | **측정 방법** | **현재 수준** |
| --- | --- | --- | --- |
| 매핑 후보 추천 정확도 (Top-1) | 70% 이상 | 정답 매핑이 추천 1순위와 정확히 일치하는 비율 | 없음 (전량 수작업) |
| 매핑 후보 추천 정확도 (Top-2 포함) | 85% 이상 | 정답 매핑이 추천 상위 2개 안에 포함되는 비율 | 없음 |
| 구조 변경(1:N) 탐지 성능 | 재현율 70% 이상, 오탐율 10% 이하 | 1:N 케이스 탐지 성공률과, 정상 1:1 케이스를 1:N으로 잘못 판단하는 오탐률을 함께 측정 | 없음 (사람이 매번 개별 판단) |
| (확장) 템플릿 SQL 생성 성공률 | 실행 성공 80% 이상 | 문법 통과·실행 성공 여부를 기준으로 측정하고, 결과 일치 여부는 참고 지표로 별도 기록 | 없음 (신규 기능) |

### 핵심 기능 정의

1. (메인) 다중 매핑 후보 추천 및 근거 제시: 개발자가 TO-BE 컬럼을 입력하면 매핑정의서에서 해당 컬럼의 AS-IS 후보를 정확 조회(Exact Lookup)로 가져오고, 후보가 여러 개면 컬럼 설명·코드 매핑·데이터 타입 일치 여부를 비교해 근거와 함께 순위를 매겨 추천한다.
2. (메인) 판단 불가 상태 처리: 근거 부족, 후보 충돌, 매핑 버전 불일치 상태를 구분해 명시하고, Agent가 임의로 하나를 확정하지 않고 사람의 판단이 필요함을 표시한다.
3. (확장) 제한된 템플릿 기반 TO-BE SQL 생성: 자유 형식의 전체 text-to-SQL이 아니라, 정해진 신고서 템플릿 범위 안에서 확정된 매핑 결과를 반영해 TO-BE SQL을 채워준다.

### 기대 효과

- 업무 효율화: 매핑 문서를 하나하나 찾아 대조하는 시간 단축
- 품질 향상: 구조 변경(1:N 등)이나 잘못된 후보 선택으로 생기는 쿼리 오류 예방
- 비용 절감: 매핑 검증에 투입되는 개발 리소스 감소, 신규 투입 인력의 온보딩 시간 단축

### 범위 및 제약사항

- In Scope: 다중 매핑 후보 추천 및 근거 제시, 판단 불가 상태 처리, 제한된 템플릿 기반 TO-BE SQL 생성(확장), 데모용 합성 스키마·매핑정의서
- Out of Scope: 자유 형식의 전체 text-to-SQL 기능(선행 기수에서 이미 다룬 주제), 실제 회사 DB/데이터 연동, 실제 매핑정의서 양식 재현, 비개발자용 UI, 운영 배포
- 제약사항: 보안상 실제 사내 DB·매핑정의서 반출이 불가하여 데모는 가상의 AS-IS/TO-BE 스키마와 매핑정의서로 진행한다. 매핑정의서처럼 정형화된 기준 정보는 유사도 검색이 아닌 정확 조회를 기본으로 하고, 컬럼 설명·업무 용어처럼 표현이 다양한 정보에서만 유사도 검색·LLM 판단을 사용한다. LLM 특성상 100% 정확도를 보장할 수 없으므로 최종 판단은 사람이 수행한다는 것을 전제로 한다.
