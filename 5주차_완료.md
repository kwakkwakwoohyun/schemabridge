## 5. PoC 모듈 구현

### 핵심 구현 내용

이번 PoC 단계에서 실제 코드로 구현된 핵심 기능들을 **동작 원리**와 **사용 기술** 중심으로 상세히 기술합니다. (SC-002 확장 기능은 SC-001 완료 후 착수 예정이라 이번 5주차 범위에서는 다루지 않습니다.)

**1.1 에이전트 워크플로우 (Agent Workflow)**

- **구현 기능:** TO-BE 컬럼명을 입력받아, 결정적 로직을 우선 적용하고 근거가 부족할 때만 LLM 판정을 더해 최종 상태(`confirmed`/`no_match`/`version_mismatch`/`insufficient_metadata`/`ambiguous`)를 반환하는 판정 파이프라인
- **동작 원리:** LangGraph `StateGraph`로 8개 노드(`lookup_mapping_candidates` → `filter_by_type` → `check_code_match` → `infer_secondary_evidence` → `judge_and_rank` → `format_response` / `handle_exception` / `await_clarification`)를 연결. 각 노드 함수가 자신의 판단 결과를 `state["route"]`에 다음 목적지 노드 이름(문자열)으로 직접 써서 반환하면, `add_conditional_edges`에 등록된 람다가 그 값을 그대로 읽어 분기하는 방식으로 라우팅을 구현함
- **주요 기술:** LangGraph(`StateGraph`, `TypedDict(total=False)` 기반 상태, `add_conditional_edges` + 명시적 `path_map`), Python 3.11

**1.2 도구(Tool) 및 함수 연동**

- **구현 기능:** (1) 매핑정의서 정확 조회 `lookup_mapping_candidates`, (2) 데이터 타입 필터 `filter_by_type`, (3) 공통코드 매핑 대조 `check_code_match`, (4) 보조 근거 스코어링 `infer_secondary_evidence`, (5) 최종 판정 `judge_and_rank`
- **동작 원리:** 앞의 세 함수는 `data/*.json`(매핑정의서·스키마·코드매핑)을 정확 대조하는 순수 결정적 로직. `infer_secondary_evidence`는 AS-IS 후보에 설명(description)이 있으면 Azure OpenAI 임베딩으로 코사인 유사도를 계산하고, 없으면 컬럼명·타입·샘플값을 묶어 Structured Output으로 LLM 자체추론을 배치 1회 호출함. `judge_and_rank`는 그 점수들의 1위-2위 차(`confidence_gap`)를 임계값과 비교하는 순수 계산 함수로, LLM을 다시 호출하지 않음
- **주요 기술:** 표준 라이브러리(`json`, `os`)만 쓰는 결정적 로직, Azure OpenAI SDK(`AzureOpenAI` 클라이언트, `chat.completions.create(response_format={"type": "json_schema", ...})` strict mode, `embeddings.create`), `python-dotenv`

**1.3 데이터 및 메모리 (RAG & Context)**

- **구현 기능:** 매핑정의서·코드매핑 같은 정형 데이터는 Vector DB 대신 정확 조회(딕셔너리 매칭)로 처리하고, 비정형 근거(컬럼 설명)에만 제한적으로 임베딩 유사도를 사용. 별도의 대화 메모리/히스토리 버퍼는 아직 없는 대신, "이미 결정적으로 확정된 다른 컬럼들의 매핑 결과"를 그때그때 모아 LLM 추론의 참고자료(few-shot 힌트)로 재사용함
- **동작 원리:** `_get_confirmed_mappings()`가 매핑정의서의 모든 항목(지금 판단 중인 컬럼은 제외)에 대해 `lookup → filter → code_match`를 다시 실행해서, 결정적으로 `confirmed`까지 떨어지는 것만 모음. 골든셋 정답 파일(`golden_set.json`)은 참조하지 않고 파이프라인이 실행 시점에 스스로 산출한 결과만 쓰기 때문에 정답 유출(cheating) 없이 힌트를 생성함
- **주요 기술:** Azure OpenAI Embeddings(`text-embedding-3-large`), 순수 Python 코사인 유사도 계산(`sum(x*y for x,y in zip(a,b))` 방식, 별도 벡터 라이브러리 미사용). 후보가 3~5개 수준으로 적어 인덱싱·저장이 필요한 Vector DB(FAISS 등)는 3주차부터 의도적으로 배제함

### 주요 문제 해결 및 기술 리서치

구현 과정에서 마주친 기술적 문제와 이를 해결하기 위해 **찾아본 자료(리서치)** 및 **적용한 방법**을 기록합니다.

| **이슈 구분** | **문제 상황 및 원인** | **리서치 및 해결 과정 (Reference & Solution)** |
| --- | --- | --- |
| **프롬프트/구조화 출력** | LLM 자체추론(`self_inference`) 결과가 후보 목록에 없는 테이블/컬럼명을 지어내거나 포맷이 깨지면, 다음 노드(`judge_and_rank`)가 그 값을 그대로 신뢰하다 오류로 이어질 위험 | • **리서치:** OpenAI Structured Output(JSON Schema strict mode) 동작 방식 확인 • **적용:** `chat_completion_json`에서 `response_format={"type": "json_schema", "json_schema": {..., "strict": True}}`로 후보별 `table`/`column`/`score`/`rationale` 필드를 스키마로 강제. `gpt-5.6-luna` 배포로 실제 호출해 정상 동작 확인(4주차 검증 완료) |
| **도구 연동** | `src/evidence.py`를 단독 스크립트로 실행(`python3 src/evidence.py`)하면 `from src.code_match import check_code_match`가 `ModuleNotFoundError`로 실패. 스크립트 자신의 위치 기준으로는 `src/`가 파이썬 경로에 없기 때문 | • **리서치:** 기존 `graph.py`/`tests/test_deterministic.py`가 이미 같은 문제를 겪어 쓰고 있던 해결 패턴 확인 • **적용:** 모듈 최상단에 `sys.path.insert(0, os.path.join(os.path.dirname(__file__), ".."))`를 추가해 프로젝트 루트를 경로에 넣음. 이후 추가한 `judge.py`에도 동일 패턴 적용 |
| **성능/기타** | `judge_and_rank`의 판정 임계값(`confidence_gap` 10%)을 설계 문서(4주차 v5) 그대로 적용했더니, 골든셋에 미리 적어둔 기대값(`ambiguous` 3건 + `insufficient_metadata` 1건)과 실제 실행 결과(4건 모두 `confirmed`)가 어긋남 | • **리서치:** 어긋난 4건의 골든셋 메모를 다시 확인해보니 `infer_secondary_evidence` 구현 전(설계 단계)에 추측으로 적어둔 기대값이었음 • **적용:** 실제 Azure OpenAI 점수 분포(gap 0.19~0.98)를 근거로, 설계 문서에 "초기값, PoC 진행하며 튜닝 예정"이라 명시된 10% 임계값을 그대로 채택. `golden_set.json`(사람이 검증한 평가 정답지)은 임의로 고치지 않고 그대로 둠(2026-08-31 결정) |

### 핵심 동작 검증

위에서 구현한 기능이 의도대로 동작하는지 보여주는 대표적인 실행 결과를 첨부합니다.

**[검증 시나리오: 설명이 비슷한 후보 2개를 임베딩 유사도로 판정 — `ACC_WHT_AGG.wht_tax_amt`]**

- **입력:** `"ACC_WHT_AGG.wht_tax_amt"`
- **에이전트 동작:**
    1. `lookup_mapping_candidates` → 매핑정의서에서 후보 2개 확인: `LBR_WHT.wht_amt`(설명 "원천징수세액"), `INT_WHT.wht_amt`(설명 "원천세액")
    2. `filter_by_type` → TO-BE 타입 `NUMERIC`과 둘 다 일치, 후보 2개 그대로 통과
    3. `check_code_match` → 둘 다 공통코드 매핑 대상이 아니라 코드값으로는 못 가림(`matched: false` × 2) → `infer_secondary_evidence`로 이동
    4. `infer_secondary_evidence` → 후보 둘 다 설명이 있어 임베딩 유사도 경로 채택. Azure OpenAI 임베딩 API를 1회 호출(TO-BE 설명 + 후보 설명 2개, 총 3문장)해 코사인 유사도 계산 → `LBR_WHT.wht_amt` = 1.0(문장 완전 일치), `INT_WHT.wht_amt` = 0.8065
    5. `judge_and_rank` → `confidence_gap` = 1.0 − 0.8065 = 0.1935(19.35%), 확정 임계값(10%) 이상이라 `confirmed` 판정
    6. `format_response` → 1위 후보 `LBR_WHT.wht_amt`로 최종 응답 생성
- **최종 결과:**

```json
{
  "to_be_column": "ACC_WHT_AGG.wht_tax_amt",
  "status": "confirmed",
  "table": "LBR_WHT",
  "column": "wht_amt",
  "reason": "보조 근거(임베딩 유사도/자체추론) 판정 결과 1위·2위 점수차가 19%로 임계값 이상이라 확정"
}
```

이 흐름을 포함해 골든셋 12건(1:1 확정 4건, LLM 판정 확정 4건, No-match 2건, Version-mismatch 1건, Insufficient-metadata 1건)이 `python3 src/graph.py`로 전부 의도한 상태로 판정됨을 확인했습니다.
