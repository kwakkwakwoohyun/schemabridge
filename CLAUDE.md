# CLAUDE.md

이 파일은 이 저장소에서 작업할 때 클로드 코드(claude.ai/code)에게 제공하는 안내 문서입니다.

## 이 저장소는 무엇인가

이 저장소는 일반적인 코드 저장소가 아닙니다. 회사 "AI Master" 8주 과정에서 한 사람이 매주 제출하는 산출물을 기록한 한글 마크다운 문서 모음이 중심이고, 4주차 설계를 바탕으로 실제 동작하는 PoC 코드(`schemabridge/`)가 하위 폴더로 함께 존재합니다. 매주 과제는 정해진 템플릿에 따라 AI Agent를 설계하고(이후 일부는 실제로 구현하는) 것입니다.

git 저장소입니다. 원격: `https://github.com/kwakkwakwoohyun/schemabridge.git` (저장소 이름은 `schemabridge`이지만, 실제로는 `AI_Master` 전체 — 주차별 문서 + `schemabridge/` 코드 — 를 이 저장소 하나에 푸시하고 있습니다). 최상위에는 문서들이 있고, `schemabridge/`만 유일한 하위 폴더입니다. `.venv/`, `__pycache__/` 등은 `.gitignore`로 제외됩니다.

## 파일 구조 및 네이밍 규칙

- `program_chapter.md` — 8주 프로그램 전체의 마스터 템플릿. 1번("역량 및 기술 스택 확인")부터 8번("최종 산출물 제출")까지, 매주 산출물이 다뤄야 할 항목을 정의합니다.
- `N주차.md` (예: `2주차.md`, `5주차.md`) — N주차의 **빈 템플릿**. `program_chapter.md`에서 해당 주차 분량만 떼어내 온 것으로, 플레이스홀더(`[...]`)가 아직 채워지지 않은 상태입니다.
- `N주차_완료.md` (예: `2주차_완료.md`) — N주차의 **작성 완료된 제출본**. `_완료.md` 파일이 있는 주차만 실제로 완료된 것이고, 빈 `N주차.md` 템플릿만 있는 주차는 아직 시작 전입니다.
- 현재 기준 2~4주차는 완료본(`_완료`)이 있고, 5~7주차는 빈 템플릿만 존재합니다. 1주차와 8주차 파일은 아직 없습니다.

"N주차 해줘" 또는 "N주차 채워줘" 같은 요청을 받으면, 섹션 템플릿은 `N주차.md`에서, 항목별 상세 지침·예시는 `program_chapter.md`에서 참고해 `N주차_완료.md`를 작성(또는 수정)합니다. 빈 템플릿인 `N주차.md` 자체는 덮어쓰지 않습니다 — 이건 채워 넣을 원본이므로 그대로 보존합니다.

## 문서 작성 규칙

- 모든 내용은 **한글**로 작성합니다. 문서를 수정·확장할 때도 이 언어를 유지합니다.
- 완료본(`_완료`) 문서는 상단에 인용구(`>`) 형태의 개정 이력을 누적합니다: `> 멘토 피드백 반영 (vN): ...`(실제 멘토 피드백 반영) 또는 `> AI 피드백 검토 후 반영 (vN): ...`(AI 피드백 중 선별 반영). 각 개정은 실제로 수정된 부분에 `**[vN 신규]**`(신규 추가) 또는 `**[vN 수정]**`(기존 수정) 같은 인라인 마커도 함께 답니다. 완료된 문서를 다시 수정할 때는 이 패턴을 그대로 따릅니다 — `> ... 반영 (vN+1): ...` 형태로 무엇을·왜 바꿨는지 요약하는 새 노트를 추가하고, 수정된 부분에 `[vN+1 신규/수정]` 태그를 달되 기존 내용을 조용히 덮어쓰지 않습니다.
- 일부 문서는 맨 끝에 짧은 "회의 대비 요약" 섹션이 있습니다 — 멘토 미팅 전에 이것만 읽어도 무엇이·왜 바뀌었는지 알 수 있게 만든 요약입니다. 이 섹션은 위쪽 개정 이력과 항상 내용이 맞아떨어지도록 유지합니다.

## 이 과정에서 설계 중인 프로젝트: SchemaBridge

이 과정(2~4주차에 걸쳐 정의됨)의 실제 주제는 **SchemaBridge**입니다. 증권사 "차세대"(핵심시스템 전면 교체) 프로젝트에서, 개발자가 기존 매핑정의서를 참고해 AS-IS(레거시) DB 컬럼을 TO-BE(신규) 컬럼에 매핑하는 작업을 돕는 LLM Agent입니다. 이 매핑이 항상 1:1이 아니고, 수작업 대조가 느리고 실수하기 쉬워서 만들게 됐습니다.

핵심 시나리오 (상세는 `3주차_완료.md` 참고):
- **SC-001 (메인):** TO-BE 컬럼이 주어지면 매핑정의서를 정확 조회(Exact Lookup)해 AS-IS 후보를 찾고, 결정적 로직을 우선 적용하는 근거 체인(데이터 타입 필터 → 코드 매핑 일치 → 설명이 있으면 유사도/없으면 자체 추론 → 제약조건 → 유사한 확정 매핑 패턴 → 컬럼명)으로 순위를 매겨, 근거와 함께 추천 결과를 반환합니다. 확신도가 낮으면 사람에게 넘기기 전에 먼저 구체적으로 되묻습니다(최대 2회 재시도). 낮은 확신도의 매핑을 스스로 확정하지 않습니다.
- **SC-002 (확장, SC-001에 의존):** SC-001에서 확정된 매핑과 사전 정의된 조인 규칙을 이용해, 특정 세금 신고서용으로 범위가 제한된(자유 형식 text-to-SQL이 아닌) TO-BE 쿼리를 생성하고, Read-only 검증 후 실행하며, 실패 시 자기수정합니다(최대 3회 재시도).

계획된 아키텍처 (`4주차_완료.md` 기준):
- **오케스트레이션:** LangGraph(`>=1.2`). 노드: `classify_intent → lookup_mapping_candidates → filter_by_type → check_code_match → infer_secondary_evidence → judge_and_rank → request_clarification(루프) → generate_rationale → format_response`, SC-002 확장 체인: `search_schema → generate_sql → validate_readonly → execute_sql → self_correct`
- **LLM/임베딩:** Azure OpenAI(GPT-5.4 계열 + `text-embedding-3-small` 계열 임베딩) — 회사가 제공하는 표준 자원이라 선택함
- **검색 전략:** 정형화된 매핑정의서는 RAG가 아닌 정확 조회. 설명·샘플값 같은 비정형 근거에만 임베딩 유사도/LLM 추론 사용. 후보가 3~5개 수준으로 적어 Vector DB는 사용하지 않고 즉석 비교함
- **출력 파싱:** Structured Output/JSON Schema. 내부 추론과 사용자 노출용 근거(rationale)를 분리
- **모니터링:** Langfuse를 LangGraph 콜백으로 연결

## `schemabridge/` 구현 현황 (중요 — 항상 최신 상태로 유지할 것)

- **이미 구현·검증 완료 (그대로 재사용, 다시 만들지 말 것):**
    - `src/data_loader.py`, `src/lookup.py`(`lookup_mapping_candidates` 정방향 + `lookup_reverse_mapping` 역방향, 2026-09-08 추가 — 아래 별도 항목 참고), `src/filters.py`(`filter_by_type`), `src/code_match.py`(`check_code_match`) — 전부 결정적(deterministic) 로직이며 API 키 없이 동작함
    - `src/llm_client.py` — Azure OpenAI 공용 클라이언트 래퍼(`chat_completion_json` Structured Output, `embed`). LLM이 필요한 노드는 전부 이 모듈을 통해서만 호출함
    - `src/evidence.py`(`infer_secondary_evidence`) — 결정적 로직으로 못 가린 후보에 임베딩 유사도/LLM 자체추론으로 점수를 매김. `_get_confirmed_mappings`로 골든셋이 아니라 파이프라인이 스스로 확정한 결과만 few-shot 힌트로 재사용(순환검증 방지)
    - `src/judge.py`(`judge_and_rank`) — `evidence_scores`로 confirmed/ambiguous/insufficient_metadata 최종 판정(`confidence_gap` 임계값 10%, top1 최소 확신도 0.5 — 둘 다 초기값, PoC 진행하며 튜닝 예정)
    - `src/clarification.py`(`request_clarification`, `MAX_ATTEMPTS=2`) — confirmed가 안 나올 때 후보/근거를 보여주는 질문을 만들고(`build_clarification_question`), 사람 답변을 반영해 LLM으로 재점수화함(`rescore_with_clarification`). "질문 생성/재점수화" 로직만 담당하고, 실제로 답을 받는 방식(터미널 `input()` vs Streamlit 위젯)은 호출하는 쪽이 각자 구현
    - `src/rationale.py`(`generate_rationale`) — confirmed 경로(단일 후보/코드값 일치/LLM 점수 확정/되묻기 후 확정 전부) 공통으로 거치는 마지막 단계. `confidence_gap` 퍼센트·`matched_keys` 같은 내부 계산값을 그대로 노출하지 않고, LLM이 사람이 읽기 좋은 근거 문장으로 바꿔서 반환함("내부 추론과 사용자 노출용 근거 분리" 원칙)
    - `src/graph.py` — LangGraph `StateGraph` 조립 완료. 노드: `lookup_mapping_candidates → filter_by_type → check_code_match → infer_secondary_evidence → judge_and_rank → generate_rationale → format_response`(confirmed) / `handle_exception`(no_match·version_mismatch·insufficient_metadata, 그리고 2회 되물어도 안 풀린 ambiguous·insufficient_metadata 최종 종료) / `request_clarification`(confirmed가 아니면 터미널 `input()`으로 실제로 되묻고 `judge_and_rank`로 재진입하는 루프, 최대 2회). `check_code_match`/`judge_and_rank`가 confirmed 판정 시 `format_response`로 바로 가지 않고 `generate_rationale`을 거쳐가도록 라우팅됨. **2026-09-08 추가**: `classify_intent`가 `as_is_column`을 채우면 `lookup_reverse_mapping → generate_reverse_rationale → format_response`(합류) 경로로, 방향조차 판별 못 하면 `direction_ambiguous`로 바로 `handle_exception`으로 라우팅됨(아래 별도 항목 참고)
    - **주의**: `request_clarification`이 실제로 동작하므로, `.venv/bin/python3 src/graph.py`(인자 없이 골든셋 전체 실행)는 `settle_method_cd`처럼 ambiguous가 나오는 케이스에서 **터미널이 멈추고 입력을 기다림**. 자동화된 배치 실행이 필요하면 특정 컬럼만 인자로 넘기거나(`.venv/bin/python3 src/graph.py ACC_WHT_AGG.pay_dt`), stdin으로 답변을 미리 파이프해야 함(예: `echo "답변" | .venv/bin/python3 src/graph.py ACC_WHT_AGG.settle_method_cd`)
    - `data/schema.json`, `data/mapping_definition.json`, `data/code_mapping.json`, `data/golden_set.json` — 합성 데모 데이터(TO-BE `ACC_WHT_AGG` 테이블, AS-IS 원천징수 테이블들). 골든셋은 **14건**: 원래 12건(사람이 검증한 SC-001 완료 기준 정답지, 고정 — 절대 수정하지 말 것) + 2026-09-05에 테스트 커버리지 갭 해소용으로 추가한 2건(`wht_reason_cd`: 코드값 disambiguation 경로 검증용, `settle_method_cd`: `judge_and_rank`의 matched_keys 우선배치·ambiguous 판정 경로 검증용). 새 2건도 기존 12건과 같은 원칙(사람이 의도를 갖고 설계한 시나리오, 시스템 자기산출 결과 아님)으로 추가됨. 전부 `ACC_WHT_AGG`/AS-IS 테이블 기준이라 아래 2026-09-07 SC-002 재설계와 무관하게 그대로 유지됨(`mapping_definition.json`도 손대지 않음). **2026-09-07 추가**: `schema.json`의 `TO-BE`에 SC-002 전용 테이블 `ACC_ETC_INCOME_AGG`/`ACC_BIZ_INCOME_AGG`를 새로 추가함(아래 SC-002 재설계 참고) — 기존 `ACC_WHT_AGG` 정의·골든셋·`mapping_definition.json`은 건드리지 않고 순수 추가만 함
    - `tests/test_deterministic.py` — 골든셋 전체를 lookup→filter→code_match 순으로 실행해 검증. 결정적으로 끝까지 판정 가능한 케이스(confirmed 5건, version_mismatch 1건, no_match 2건, insufficient_metadata 1건)는 실제 상태와 일치, 나머지(LLM 판단 필요한 케이스)는 의도대로 "PENDING → 다음 단계 필요"로 넘어감
    - `.venv/bin/python3 src/graph.py <컬럼>`(개별 컬럼, Azure OpenAI 키 필요) 실행 시 골든셋 14건 각각 의도한 범주로 판정됨을 확인함(no_match 2건, version_mismatch 1건, insufficient_metadata 1건은 호출마다 항상 동일 — 나머지는 confirmed 또는 ambiguous). 단, `payee_biz_no`는 LLM 자체추론 점수가 임계값 근처(0.5~0.6대)라 호출마다 `confirmed`/`ambiguous`가 갈릴 수 있음(비결정적 LLM 호출 특성 — 버그 아님). `settle_method_cd`는 도움되는 답을 주면 1회 만에 `confirmed`로, 도움 안 되는 답을 반복하면 2회 후 `handle_exception`(최종 ambiguous)으로 정직하게 종료됨을 실제로 확인함(`request_clarification` 루프 검증용 케이스)
    - **SC-001 역방향(AS-IS→TO-BE) 조회, 2026-09-08 추가.** 원래 SC-001은 TO-BE 컬럼이 주어지면 AS-IS 후보를 찾는 정방향만 지원했는데, "이 AS-IS 컬럼(예: `LBR_WHT.wht_amt`) 매핑되는 TO-BE 컬럼 찾아줘"처럼 반대 방향 질문(식별자든 자연어든)도 지원해야 한다는 사용자 지적으로 추가함. `src/intent.py`(`classify_intent`)가 TO-BE 스키마뿐 아니라 AS-IS 스키마도 프롬프트에 같이 받아, 정방향이면 `to_be_column`을, 역방향이면 `as_is_column`을 채움(`INTENT_SCHEMA`에 `as_is_column` 필드 추가). `src/lookup.py`(`lookup_reverse_mapping(as_is_table, as_is_column)`)이 매핑정의서 entries를 스캔해 이 AS-IS 컬럼을 candidates로 갖는 entry를 찾는 역인덱스 조회를 수행함 — 정방향과 달리 AS-IS 컬럼 하나가 둘 이상의 TO-BE 컬럼에 걸치는 경우가 현재 데이터엔 0건이라, `filter_by_type`~`judge_and_rank` 판정 파이프라인을 타지 않고 매치 1건이면 바로 확정함(`src/graph.py`의 `generate_reverse_rationale_node`가 기존 `generate_rationale`을 그대로 재사용해 근거 문장을 만든 뒤 `format_response`로 합류, `direction` 필드로 정방향/역방향 구분 노출). 매치 0건이면 `reverse_no_match`, 2건 이상이면 `reverse_ambiguous`로 `handle_exception` 종료. **컬럼명만으로 방향/테이블조차 특정 못 하는 실제 케이스가 있음**(예: `settle_method_cd`는 TO-BE `ACC_WHT_AGG`와 AS-IS `LBR_WHT`/`INT_WHT` 양쪽에 다 있고, `acct_nm`은 AS-IS `INT_WHT`/`DIV_WHT` 두 테이블에 걸침) — 이 경우 `classify_intent`가 억지로 추측하지 않고 `to_be_column`/`as_is_column`을 둘 다 null로 반환하도록 프롬프트에 명시했고, `classify_intent_node`가 이를 `direction_ambiguous`로 바로 `handle_exception`에 보내 사용자에게 테이블명을 포함해 다시 질문해달라고 안내함(SC-001의 기존 "확신 없으면 스스로 확정하지 않는다" 원칙을 방향 판별에도 그대로 적용한 것). `app.py`도 `run_sc001_reverse(as_is_column)`을 추가하고 버튼 클릭 디스패치가 `to_be_column`/`as_is_column`/방향불명 세 갈래로 분기하도록 반영함. **검증 완료**: `tests/test_reverse_lookup.py`(신규, `lookup_reverse_mapping` 결정적 케이스 5건 API 키 없이 통과 + `classify_intent` 방향 판별 + 전체 그래프 체인, 자연어 질문 "LBR_WHT.wht_amt가 TO-BE 어디로 매핑돼?"로 실제 `ACC_WHT_AGG.wht_tax_amt` 확정까지 확인), `tests/test_app_streamlit.py`에 `test_sc001_reverse_lookup` 추가. 기존 골든셋 14건 정방향 회귀(`test_deterministic.py`, `src/graph.py <컬럼>`)와 SC-002 전체(`test_sc002.py`) 재실행해 회귀 없음을 확인함
    - `app.py` — Streamlit 기반 **시연 영상용 뷰어**(제품 기능 아님, 2주차 범위정의상 "비개발자용 UI"는 Out of Scope로 명시했으므로 이것과 혼동하지 말 것). SC-001과 SC-002가 하나의 프로젝트라서 입력창 하나로 둘 다 받음 — `graph.py`(CLI)와 똑같이 `classify_intent()`로 먼저 분류한 뒤 갈라짐(2026-09-05 결정, 처음엔 SC-001 전용으로 만들었다가 "하나의 프로젝트로 SC-001/SC-002를 모두 시연해야 한다"는 사용자 지적으로 확장함). **SC-001 정방향 경로**: 결정적 로직 → (필요시) `infer_secondary_evidence` → `judge_and_rank`까지 그대로 호출해 최종 상태를 실시간으로 보여주고, confirmed가 아니면 `st.session_state` 기반으로 `request_clarification`까지 실제로 되물어 재판정함(최대 2회). 어느 경로로 confirmed가 나오든(단일 후보/코드값 일치/LLM 점수 확정/되묻기 후 확정) `generate_rationale`을 호출해, 화면에는 내부 판정 과정(1차 판정·AS-IS 후보·근거 스코어링 표)과 최종 근거 문장을 함께 보여줌. **SC-001 역방향 경로(2026-09-08 추가)**: `run_sc001_reverse`가 `lookup_reverse_mapping`만 호출해 후보 표/스코어링 표 없이 바로 confirmed 근거 또는 no_match/ambiguous 메시지를 보여줌. **SC-002 경로**: `search_schema`(조인 규칙 조회 + SC-001 도구로 재검증) → `generate_sql` → `validate_readonly` → `execute_sql`을 그대로 호출하고, 실패 시 `generate_sql`로 되돌아가는 self_correct 재시도(최대 `MAX_SQL_ATTEMPTS`회)를 사람 개입 없이 버튼 클릭 한 번 안에서 자동으로 돔(`request_clarification`과 달리 사람 답변이 필요 없는 루프라 rerun 없이 그 자리에서 끝까지 처리 가능). 성공하면 생성된 SQL과 실행 결과 표를, 실패하면 `mapping_not_ready`/`readonly_violation`/`sql_execution_failed` 중 하나를 보여줌. `streamlit run app.py`로 실행. **검증 완료(2026-09-05)**: `streamlit.testing.v1.AppTest`로 SC-001 식별자 입력/SC-001 자연어 질문/SC-002 정상 리포트 생성/`request_clarification` 되묻기 루프(`settle_method_cd`, 답변 제출까지)/SC-002 예외 3종(각각 `src.intent.classify_intent`·`src.schema_search.search_schema`·`src.sql_generation.generate_sql`을 mock해서 강제 재현) 전부 예외 없이 렌더링되는 것까지 확인함
    - **2026-09-09 추가 — 진행 상황 표시를 `st.spinner` → `st.status`로 교체.** 시연 영상을 찍어보니, 기존엔 `st.spinner("X 실행 중...")`가 단계마다 메시지 하나만 잠깐 떴다가 사라져서 전체 처리 흐름이 화면에 안 남는다는 사용자 지적을 받음("클로드 코드처럼 이거 하고있다 저거 하고있다가 계속 아래로 쌓였으면 좋겠다"). `run_sc001`/`run_sc001_reverse`/`run_sc002`가 이제 `status`(`st.status()` 컨테이너) 인자를 받아 각 단계(결정적 로직 포함 — `lookup_mapping_candidates`/`lookup_reverse_mapping`/`search_schema`/`filter_by_type`/`check_code_match`부터 LLM 단계까지 전부)마다 `status.write(...)`로 한 줄씩 남기도록 바꿈(스피너처럼 지워지지 않고 아래로 계속 쌓임). 버튼 클릭 핸들러(`실행`, `답변 제출` 둘 다)가 각각 자기 `st.status(...)` 블록을 만들어 그 안에서 호출. 범위는 "한 번의 질문 처리 과정만"으로 한정함(권장 옵션 채택) — 여러 질문에 걸친 채팅형 누적 로그(세션 전체 이력을 계속 쌓아 보여주는 것)는 화면 구조 자체를 바꿔야 해서 이번 범위에서 제외. `tests/test_app_streamlit.py`로 회귀 없음 확인(`request_clarification` 되묻기 루프까지 포함해 예외 없이 렌더링됨).
    - **SC-002(신고서용 집계 쿼리 생성, 2026-09-05 구현, 2026-09-07 아키텍처 재설계) — `src/intent.py`(`classify_intent`, LLM)** 그래프의 첫 진입점. 입력의 겉모습(식별자 vs 문장)이 아니라 **의미**로 분류함 — "wht_tax_amt가 어디서 오는지 알려줘"처럼 자연어 문장이어도 컬럼 하나의 매핑을 묻는 거면 SC-001, 여러 컬럼을 모은 집계·리포트 요청이면 SC-002. TO-BE 스키마(컬럼명+설명)를 프롬프트에 같이 줘서 자연어로 언급된 컬럼도 실제 식별자로 정규화함(예: "원천징수세액"→`ACC_WHT_AGG.wht_tax_amt`). **주의**: 처음엔 "식별자면 SC-001, 문장이면 SC-002"라는 형태 기준으로 짰다가, 자연어로 된 컬럼 매핑 질문까지 SC-002로 잘못 분류되는 걸 사용자가 지적해서 의미 기준으로 다시 짬(2026-09-05).
    - **2026-09-07 추가 — SC-002 안에서 "탐색"과 "실행"을 구분하는 `sc002_mode`.** 처음엔 SC-002로 분류되기만 하면 무조건 `search_schema → generate_sql → execute_sql`까지 끝까지 실행해서 SQL과 결과를 보여줬는데, "원천세 집계 관련된 테이블 다 찾아줘"처럼 실제로는 관련 테이블/컬럼 정보만 알고 싶은 탐색 요청까지 매번 쿼리를 만들어 실행해버리는 게 사용자 지적으로 드러남. 그래서 `classify_intent`가 SC-002일 때 `sc002_mode`("explore" 또는 "execute")도 함께 분류하도록 확장함(`src/intent.py` `INTENT_SCHEMA`에 필드 추가). `src/graph.py`의 `search_schema_node`가 `sc002_mode == "explore"`면 새 노드 `format_schema_response`로 라우팅해 `search_schema`가 이미 검증해 둔 테이블/컬럼 정보(`join_rule.sources`)만 `final_answer`(`status: "schema_found"`)로 보여주고 끝냄(`generate_sql`/`execute_sql` 호출 안 함). `execute`(기본값)면 기존 흐름 그대로 진행. `app.py`도 `run_sc002(user_request, sc002_mode)`로 시그니처가 바뀌어 같은 분기를 재구현하고, `render_report_result`에 `status == "schema_found"` 렌더링 분기(SCHEMA_FOUND, 테이블별 컬럼 매핑 나열)를 추가함.
    - **2026-09-07 재설계 — "AS-IS 실시간 조인" → "이미 마이그레이션된 TO-BE 테이블 조회"로 전환.** 처음 버전(2026-09-05)은 `join_rules.json`에 (AS-IS 테이블.컬럼 → TO-BE 컬럼) `column_map`을 두고, `search_schema`가 각 쌍을 SC-001 전체 파이프라인(`filter_by_type → check_code_match → infer_secondary_evidence → judge_and_rank`)으로 재검증한 뒤, `generate_sql`이 AS-IS 테이블(`ETC_INCOME`/`BIZ_INCOME`)을 매 쿼리마다 `UNION ALL`로 직접 합치는 "실시간 ETL형" 쿼리를 만들었다. 사용자가 지적하길, "차세대"는 마이그레이션이 끝나면 TO-BE 테이블에 실제 데이터가 이미 들어있는 게 맞는 그림이고, 실무에서도 소득유형별로 TO-BE 테이블이 따로 관리되는 경우가 있다고 해서(2026-09-07), 다음과 같이 바뀜:
        - `data/schema.json`의 `TO-BE`에 `ACC_ETC_INCOME_AGG`(기타소득)·`ACC_BIZ_INCOME_AGG`(사업소득) 테이블을 새로 추가. 기존 `ACC_WHT_AGG`(SC-001이 쓰는 테이블)는 이 리포트에서 배당소득 역할을 겸함 — 총 3개 TO-BE 테이블
        - `data/join_rules.json`의 `sources`가 더 이상 AS-IS 테이블이 아니라 이 3개 TO-BE 테이블을 가리킴(`report_id`도 `wht_report_div_etc_biz_income`, `report_name`도 "원천세 신고서 (배당·기타·사업소득)"으로 확장)
        - **`src/schema_search.py`(`search_schema`)** 검증 로직의 성격이 바뀜 — 소스 자체가 이미 TO-BE 확정 테이블이라 "이 AS-IS 컬럼이 정말 이 TO-BE 컬럼과 같은 의미인가"를 LLM으로 판정할 필요가 없어짐. 그래서 SC-001의 LLM 판정 단계(`check_code_match` 이후)는 더 이상 부르지 않고, 결정적 도구인 `filter_by_type`만 재사용해 "각 소스 테이블의 컬럼이 `schema.json`에 실제로 존재하고, 리포트 기준 컬럼(첫 번째 소스)과 타입이 맞는가"라는 구조적 일관성만 확인함. 하나라도 없거나 타입이 안 맞으면 리포트 전체를 `mapping_not_ready`로 막는 원칙은 그대로 유지
        - **`src/sql_generation.py`(`generate_sql`, LLM)** 프롬프트가 "3개 TO-BE 테이블을 `UNION ALL`로 세로로 합쳐라(조인 아님 — 각 테이블이 이미 리포트에 필요한 컬럼을 다 갖고 있음)"로 바뀜
        - **`data/setup_demo_db.py`** 이제 AS-IS `ETC_INCOME`/`BIZ_INCOME` 대신 TO-BE `ACC_WHT_AGG`/`ACC_ETC_INCOME_AGG`/`ACC_BIZ_INCOME_AGG` 3개 테이블에 각 12건씩(배당/기타/사업소득, 총 36건. 2026-09-07 추가 확장 — 아래 기간 필터 항목 참고) "이미 마이그레이션된" 합성 데이터를 시딩함
        - `src/sql_validation.py`(`validate_readonly`)·`src/sql_executor.py`(`execute_sql`, `data/demo.db`에 `PRAGMA query_only=ON`으로 이중 방어하며 실행, 실패 시 `generate_sql`로 되돌아가는 self_correct 루프 최대 3회 후 `handle_exception`)는 변경 없이 그대로 재사용
    - 이 범위(Vertical Slice)는 위 3개 TO-BE 테이블(배당·기타·사업소득)을 `UNION ALL`로 합쳐 지급일자/지급처명/소득구분/소득금액/원천징수세액 5개 고정 항목을 뽑는 리포트 하나만 지원. `generate_sql` 프롬프트는 "사용자 요청에 특정 소득유형이 지정되어 있으면 해당 테이블만, 없으면 전체를 `UNION ALL`"로 되어 있어 무조건 3개 전부를 합치지는 않음(2026-09-07 수정 — 처음엔 항상 3개 전부 `UNION ALL`하게 만들었다가 사용자가 "요청에 따라 달라져야지 무조건 UNION ALL은 아니다"라고 지적해서 바로잡음, `tests/test_sc002.py`의 `test_sc002_single_income_type`으로 "사업소득만 뽑아줘" 요청이 실제로 단일 테이블 SELECT만 생성하는지 검증함)
    - **2026-09-07 추가 — 기간(WHERE) 필터.** 처음엔 `report_columns`에 `pay_dt`(지급일자)가 없어서 생성되는 쿼리에 WHERE절 자체가 나올 여지가 없었고(신고서는 원래 항상 분기/연도 단위인데도), 데모 데이터도 테이블당 5건이 전부 2026년 1분기에만 몰려 있어서 기간 조건을 걸어도 결과가 항상 똑같았음 — 사용자가 "쿼리가 너무 단조롭다, WHERE문 걸 수 있는 걸 생각해보라"고 지적해서 확장함. `data/join_rules.json`의 `report_columns`/각 소스 `column_map`에 `pay_dt` 추가, `data/setup_demo_db.py` 데이터를 2025년 4분기·2026년 1분기·2026년 2분기 각 4건(테이블당 12건, 총 36건)으로 확장, `src/sql_generation.py` 프롬프트에 오늘 날짜를 근거로 줘서 "2026년 1분기"/"지난 분기"/"이번 달" 같은 표현이 나오면 `pay_dt`('YYYY-MM-DD' TEXT, 문자열 비교로 범위 필터 가능)에 대한 WHERE 조건을 추가하고 기간 언급이 없으면 WHERE 없이 전체를 조회하도록 지시함. `tests/test_sc002.py`의 `test_sc002_period_filter`로 "2026년 1분기" 같은 절대 기간 표현이 실제로 해당 분기 데이터(12건)로만 좁혀지는 것까지 확인함(상대 표현 "이번 분기"는 테스트 실행 시점의 실제 오늘 날짜에 좌우돼서 회귀 테스트엔 절대 표현을 씀)
    - `src/graph.py` 엣지: `START → classify_intent → (SC-001) lookup_mapping_candidates`(이하 기존과 동일) `/ (SC-002) search_schema → (sc002_mode=explore) format_schema_response(END) / (sc002_mode=execute) generate_sql → validate_readonly → (통과) execute_sql / (위반) handle_exception` → `execute_sql → (성공) format_report_response / (실패, 재시도<3) generate_sql / (재시도≥3) handle_exception`. `main()`의 상태 시딩 키가 `to_be_column`에서 `user_request`로 바뀜(SC-001 컬럼명 입력은 그대로 동작). SC-002 CLI 예시(execute): `.venv/bin/python3 src/graph.py "이번 분기 원천세(배당·기타·사업소득) 신고서용 집계 데이터 뽑아줘"` / (explore): `.venv/bin/python3 src/graph.py "원천세 집계 관련된 테이블 다 찾아줘"`
    - `tests/test_sc002.py` — `validate_readonly`/DB 시딩(2026-09-07 기준 `ACC_WHT_AGG`/`ACC_ETC_INCOME_AGG`/`ACC_BIZ_INCOME_AGG` 각 12건, 3개 분기 분산)/`search_schema`(결정적, API 키 불필요) + `classify_intent`(SC-001/SC-002 execute/SC-002 explore 3가지 모두 검증)/SC-002 전체 체인(execute 기간 미지정 일반 요청 36건·execute 특정 소득유형만 12건·execute 절대 기간 필터 12건·explore, LLM, 키 필요) 검증. 전체 통과 확인됨(mapping_not_ready·readonly_violation·self_correct 3회 소진 경로는 `unittest.mock`으로 별도 확인, 정상 성공 경로는 실제 SQLite 결과가 시딩 데이터와 정확히 일치함을 확인)
    - `tests/test_app_streamlit.py` (2026-09-05 신규, 2026-09-07 explore 케이스 추가) — `streamlit.testing.v1.AppTest`로 `app.py`를 브라우저 없이 검증(실제 버튼 클릭/텍스트 입력 시뮬레이션). SC-001 식별자 입력/자연어 질문/`request_clarification` 되묻기 루프(`settle_method_cd`, 답변 제출까지)/SC-002 정상 리포트 생성(execute)/SC-002 예외 3종(mock)/SC-002 탐색 모드(explore, SCHEMA_FOUND 렌더링) 여섯 케이스 전부 통과 확인. `app.py` 내부 함수를 mock할 땐 `patch("app.함수이름", ...)`이 아니라 `patch("src.모듈.함수이름", ...)`으로 원본을 패치해야 함(AppTest가 스크립트를 매 `run()`마다 재실행하기 때문) — 상세는 `schemabridge/src/CODE_GUIDE.md` 참고
- 남은 것: `data/setup_demo_data.py`(이미 `data/setup_demo_db.py`로 구현됨, 계획 당시 이름과 다름), `src/cli.py`, `tests/run_eval.py` 같은 엔트리포인트는 여전히 없음 (4주차 설계 문서상의 계획일 뿐, 실제 파일 아님). 이걸로 SC-001/SC-002 모두 4주차 설계 노드가 전부 구현됨.
- 이미 구현된 결정적 로직/기존 골든셋 12건/SC-002 `join_rules.json`에 손대야 할 이유가 생기면, 먼저 사용자에게 확인하고 진행할 것 — 이미 여러 차례 검증을 거친 코드임.

## `schemabridge/` 실행 및 테스트

- `schemabridge/requirements.txt`에 실제로 import되는 직접 의존성만 핀 고정(`langgraph`, `openai`, `python-dotenv`, `streamlit` — `langchain-openai`는 실제 코드 어디서도 import하지 않으므로 포함 안 함, Azure OpenAI 호출은 `openai.AzureOpenAI`를 직접 씀). `pyproject.toml`은 아직 없음(패키징이 필요 없는 PoC라 당장은 불필요, `pip install -r requirements.txt`로 충분). 결정적 로직(`data_loader`/`lookup`/`filters`/`code_match`)은 표준 라이브러리(`json`, `os`)만 쓰므로 이 설치 없이도 바로 실행됨.
- 결정적 파이프라인 검증 스크립트 실행(사실상 유일한 "테스트"):
  ```
  cd schemabridge && .venv/bin/python3 tests/test_deterministic.py
  ```
  pytest 기반이 아니라 골든셋 14건을 돌려 표로 출력하는 스크립트이므로, 개별 케이스만 보려면 파일 하단의 `["ACC_WHT_AGG.income_type_cd", ...]` 목록을 수정하거나 `main()`을 직접 호출해 특정 컬럼만 확인하세요.
- SC-002 검증 스크립트: `cd schemabridge && .venv/bin/python3 tests/test_sc002.py` (앞부분 `validate_readonly`/DB 시딩/`search_schema`는 결정적이라 API 키 없이도 통과, `classify_intent`/전체 체인은 Azure OpenAI 키 필요 — 키가 없으면 `SCHEMABRIDGE_SKIP_LLM_TESTS=1`로 결정적 부분만 실행). SC-002가 실행에 필요한 `data/demo.db`는 `data/setup_demo_db.py`가 최초 실행 시 자동으로 만들어줌(SQLite, 표준 라이브러리 `sqlite3`만 사용 — 추가 패키지 설치 불필요).
- SC-001 역방향(AS-IS→TO-BE) 검증 스크립트(2026-09-08 신규): `cd schemabridge && .venv/bin/python3 tests/test_reverse_lookup.py` (`lookup_reverse_mapping` 결정적 케이스는 API 키 없이도 통과, `classify_intent` 방향 판별/전체 그래프 체인은 Azure OpenAI 키 필요 — 키가 없으면 `SCHEMABRIDGE_SKIP_LLM_TESTS=1`로 결정적 부분만 실행).
- Streamlit 데모(`app.py`) 검증 스크립트: `cd schemabridge && .venv/bin/python3 tests/test_app_streamlit.py` (전부 LLM 호출 포함 — 키 없으면 `SCHEMABRIDGE_SKIP_LLM_TESTS=1`로 전체 건너뜀). `streamlit` 패키지(`requirements.txt`에 포함)가 설치돼 있어야 함.
- **중요 — Python 버전 주의:** `src/data_loader.py`가 `dict | None` (PEP 604) 반환 타입 문법을 쓰기 때문에 **Python 3.10 이상**이 필요합니다. 이 머신의 기본 `python3`는 `/usr/bin/python3` (3.9.6)이며, 이 버전으로 실행하면 모듈 임포트 시점에 `TypeError: unsupported operand type(s) for |: 'type' and 'NoneType'`로 즉시 실패합니다. **2026-09-07 기준 확인**: `schemabridge/.venv`에 Python 3.11.16 가상환경이 이미 만들어져 있고 `requirements.txt`의 의존성(`langgraph`/`openai`/`streamlit` 등)도 전부 설치되어 있음 — 실행·테스트는 `schemabridge/.venv/bin/python3 ...`로 하면 됨(Homebrew `python3.11`도 `/opt/homebrew/bin/python3.11`에 별도로 존재). 이 venv가 없는 새 환경이라면 3.10+ 인터프리터 확보 방법(pyenv 설치, Homebrew `python@3.11` 등)을 사용자와 먼저 확인하세요 — 임의로 `dict | None`을 `Optional[dict]`로 바꾸는 등 코드를 낮은 버전에 맞춰 되돌리지 마세요(4주차 설계 문서 기준 Python 3.11+가 명시적 전제입니다).

## 이 저장소에서 작업할 때

- 문서 작업(`N주차_완료.md`)과 `schemabridge/` 코드 작업은 성격이 다릅니다. 관련 요청을 받으면 사용자가 실제로 원하는 게 "N주차 문서 작성/수정"인지 "`schemabridge/` 코드 작업(SC-001/SC-002는 2026-09-05 기준 4주차 설계 노드가 전부 구현됨)"인지 먼저 확인하세요.
- SchemaBridge를 실제로 구현해달라는 요청을 받으면, `4주차_완료.md`의 "개발 환경 구축"과 기술 스택 표를 기준 스펙으로 삼고, 이미 확정된 선택(Azure OpenAI, LangGraph, Vector DB 미사용 등)에서 벗어나기 전에 반드시 사용자에게 먼저 확인하세요 — 여러 차례 멘토 피드백을 거쳐 확정된 내용입니다.
